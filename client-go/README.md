# Sports Statistics API — Go client (CLI)

A CLI client for the MAXI Sports Statistics API, mirroring `client-py`.

## Run locally

```bash
cd maxi-examples/client-go
BASE_URL=http://localhost:8050 go run .
```

## Docker

```bash
docker compose --profile clients up client-go
```
